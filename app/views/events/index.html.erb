<h1>Events
  <%= link_to 'New Event', new_event_path, :class => "btn btn-primary" %></h1>

<%= render :partial => 'published_events' %>
<%= render :partial => 'draft_events' if user_signed_in? && current_user.admin %>

<br />

<%= link_to 'New Event', new_event_path %>
